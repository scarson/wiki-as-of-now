// ABOUTME: Core app identity — exports the canonical application name.
// ABOUTME: Used by tests and any runtime code that needs to identify this app.
export function appName(): string { return "WikiAsOfNow"; }
